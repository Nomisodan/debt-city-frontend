# Copy to .env.local for local dev (leave blank — Vite proxy handles /api)
VITE_API_URL=

# In production, set this to your backend's deployed URL, e.g.:
# VITE_API_URL=https://debt-city-backend-production.up.railway.app
